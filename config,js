/*
  CONFIGURACIÓN DEL PORTAL

  Después de desplegar la Cloud Function
  consultarPulseraPublica, reemplaza la URL inferior.

  Ejemplo Gen 2 / Cloud Run:
  https://consultarpulserapublica-xxxxxxxx-tl.a.run.app
*/

export const PORTAL_CONFIG = Object.freeze({
  apiUrl:
    "PEGAR_URL_CLOUD_FUNCTION_AQUI",

  sessionTokenKey:
    "raitrai_nfc_session_token_v2",

  selectedGroupKey:
    "raitrai_nfc_selected_group_v2"
});
